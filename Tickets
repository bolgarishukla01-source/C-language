#include<stdio.h>
int main()
{
  int age ;
  printf("Enter age : ");
  scanf("%d", &age);
  if (age <= 3 || age >= 100 )
  {
    printf(" Ticket Price = 0\n");
  }
  else if (age <= 13 || age >= 60 )
  {
    printf(" Ticket Price = 5\n");
  }
  else 
  {
    printf(" Ticket Price = 10\n");
  }
  return 0;
}
