#include <stdio.h>
void main()
{
  int hour;
  printf("Enter hour : ");
  scanf("%d" , &hour);
  if (hour>=0 && hour <= 11)
  {
    printf("%dAM\n",hour);
  }
  else if (hour>=12 && hour<=23)
  {
    hour = hour-12;
    printf("%dPM\n",hour);
  }
  else
  {
    printf("Invalid Hour !!\n");
  }
}
